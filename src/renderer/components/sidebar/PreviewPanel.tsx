/**
 * Legacy path — Preview UI moved to activity bar + content panel (rail migration).
 * @deprecated Import from `../preview/PreviewContentPanel` instead.
 */
export { PreviewContentPanel as PreviewPanel, PreviewContentPanel } from "../preview/PreviewContentPanel";

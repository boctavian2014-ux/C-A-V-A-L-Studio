Windows icon source: `branding/app-icon.svg`.

Required output for electron-builder:

- `icon.ico`

Generate from SVG using an icon pipeline such as `electron-icon-builder`, ImageMagick, or a design export.

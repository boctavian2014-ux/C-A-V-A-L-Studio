# Build System

Documentatia completa pentru build si release pipeline este in `installer/docs/build-system.md`.

Include:

- arhitectura build system;
- CI/CD pipeline;
- release channels;
- signing si notarizare;
- update feeds si delta updates.
